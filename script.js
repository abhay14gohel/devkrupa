var element = document.getElementById("myElement");
var width = element.offsetWidth; // Get the current width of the element
var height = width ; // Calculate the desired height as 75% of the width
element.style.height = height + "px"; // Apply the height to the element
